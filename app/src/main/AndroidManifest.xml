<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.cb.qiangqiang2">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>

    <application
        android:name=".common.application.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.BoardDragEditActivity"
            android:label="@string/board_edit"
            android:theme="@style/ActivityTheme"/>
        <activity
            android:name=".ui.activity.WebViewActivity"
            android:label="@string/title_activity_web"
            android:theme="@style/ActivityTheme"/>
        <activity
            android:name=".ui.activity.UserInfoActivity"
            android:label="@string/title_activity_person_info"
            android:theme="@style/ActivityTheme"/>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:launchMode="singleTop"/>

        <!-- For AutoLayout lib -->
        <meta-data
            android:name="design_width"
            android:value="768"/>
        <meta-data
            android:name="design_height"
            android:value="1280"/>

        <!-- test activity star -->
        <activity
            android:name=".test.activity.MainTestActivity"
            android:theme="@style/ActivityTheme"/>
        <activity
            android:name=".test.activity.Activity2"
            android:theme="@style/ActivityTheme"/>
        <activity android:name=".test.activity.CustomViewActivity"/>
        <activity
            android:name=".test.activity.ContentProviderActivity"
            android:theme="@style/ActivityTheme"/>
        <!-- test activity end -->

    </application>

</manifest>